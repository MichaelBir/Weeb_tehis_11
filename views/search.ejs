<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Search</title>
</head>
<body>
    <div class="container">
        <div class="form-container">
            <form action="/search" method="POST">
                <input type="text" name="movieTitle" placeholder="Find a movie">
                <button type="submit">Search</button>
            </form>
        </div>
        <% if(movieDetails) { %>
            <div class="result-container">
                <ul class="movie-data-list">
                    <li><span>Title: <%= movieDetails.title %></span></li>
                    <li><span>Year:  <%= movieDetails.year %></span></li>
                    <li><span>Genres: <%= movieDetails.genres %></span> </li>
                    <li ><span>Overview: <p class="overview"> <%= movieDetails.overview %></p></span></li>
    
                </ul>
    
                <div>
                    <img src="<%= movieDetails.posterUrl %>" alt="">
                </div>
            </div>
        <% } %>
    </div>
    
</body>
</html>